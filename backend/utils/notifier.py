def notify_user(user_id, message):
    # Placeholder: You can integrate Twilio, Email, or Firebase Messaging
    print(f"[NOTIFY USER {user_id}] {message}")

def notify_serviceman(serviceman_id, message):
    # Placeholder: You can integrate real push notifications here
    print(f"[NOTIFY SERVICEMAN {serviceman_id}] {message}")
